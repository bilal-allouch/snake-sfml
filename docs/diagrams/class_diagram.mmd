classDiagram
direction LR

class Game {
  -sf::RenderWindow& m_window
  -Renderer m_renderer
  -Snake m_snake
  -Food m_food
  -std::mt19937 m_rng
  -sf::Font m_font
  -sf::Text m_hudText
  -sf::Text m_startText
  -sf::Text m_pausedText
  -sf::Text m_gameOverText
  -sf::RectangleShape m_overlay
  -GameState m_state
  -int m_score
  -int m_applesEaten
  -sf::Clock m_clock
  -sf::Time m_accumulator
  -int m_baseTickMs
  -int m_minTickMs
  -int m_msStep
  +Game(sf::RenderWindow&)
  +run()
  -processEvents()
  -update(sf::Time)
  -render()
  -resetToStart()
  -startNewRun()
  -restart()
  -setState(GameState)
  -isOutside(Cell) bool
  -randomFreeCell() Cell
  -eatApple()
}

class Renderer {
  -sf::RenderWindow& m_window
  -CELL_SIZE int
  -GRID_WIDTH int
  -GRID_HEIGHT int
  +Renderer(sf::RenderWindow&)
  +renderGrid()
  +renderSnake(Snake)
  +renderFood(Food)
  +cellSize() int
  +gridWidth() int
  +gridHeight() int
}

class Snake {
  -std::vector~Cell~ m_body
  -Direction m_dir
  -int m_grow
  +Snake()
  +reset()
  +body() const std::vector~Cell~&
  +head() const Cell&
  +setDirection(Direction)
  +direction() const Direction
  +nextHead() const Cell
  +move()
  +grow(int)
  +contains(Cell) const bool
  +isSelfColliding() const bool
}

class Food {
  -Cell m_pos
  +Food()
  +position() const Cell&
  +setPosition(Cell)
}

class Cell {
  +int x
  +int y
  +operator==(Cell) bool
}

class Direction {
  <<enumeration>>
  Up
  Down
  Left
  Right
}

class GameState {
  <<enumeration>>
  Start
  Playing
  Paused
  GameOver
}

Game --> Renderer : uses
Game --> Snake : owns
Game --> Food : owns
Renderer ..> Snake : renders
Renderer ..> Food : renders
Snake --> Cell : body cells
Food --> Cell : position
Snake ..> Direction : movement
Game ..> GameState : state
Game ..> sf::RenderWindow : window ref
Renderer ..> sf::RenderWindow : window ref
